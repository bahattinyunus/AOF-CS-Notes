# İşletim Sistemleri Yönetimi - Özet

**Kategori:** Temel Bilişim / Sistem
**Konu:** İşletim Sistemi Kavramları, Windows & Linux Komutları

---

## 🧠 1. İşletim Sistemi (OS) Nedir?
Donanım ile kullanıcı/yazılım arasında köprü kuran ana yazılımdır. Kaynakları (CPU, RAM) yönetir.
*   **Örnekler:** Windows, Linux (Ubuntu, Kali), macOS, Android.

## ⚙️ 2. Temel Kavramlar

### Process (Süreç)
Çalışmakta olan bir programa denir. Her sürecin bir ID'si (PID) vardır.

### Thread (İş Parçacığı)
Bir sürecin alt işleridir. (Örn: Word'de yazı yazarken arka planda imla denetimi yapılması).

### Deadlock (Kilitlenme)
İki sürecin birbirinin kaynağını beklemesi ve sistemin donması durumudur. (Trafik sıkışıklığı gibi).

## 🐧 3. Linux Temel Komutları
Bir sistem yöneticisinin bilmesi gereken "Terminal" komutları:

| Komut | Açıklama |
| :--- | :--- |
| `ls` | Klasördeki dosyaları listeler (List). |
| `cd` | Klasör değiştirir (Change Directory). |
| `pwd` | Hangi klasörde olduğunu gösterir. |
| `mkdir` | Yeni klasör oluşturur. |
| `rm` | Dosya siler (Remove). |
| `cp` | Kopyalar (Copy). |
| `mv` | Taşır veya isim değiştirir (Move). |
| `sudo` | Yönetici (Root) yetkisiyle işlem yapar. |
| `top` | Çalışan süreçleri gösterir (Görev Yöneticisi). |

## 🪟 4. Windows CMD Komutları

| Komut | Açıklama |
| :--- | :--- |
| `dir` | Dosyaları listeler. |
| `ipconfig` | IP adresini gösterir. |
| `ping` | Bağlantı testi yapar. |
| `tasklist` | Çalışan işlemleri gösterir. |
| `cls` | Ekranı temizler. |

> **💡 Sınav İpucu:**
> Linux **büyük/küçük harf duyarlıdır** (Case Sensitive). `Dosya.txt` ile `dosya.txt` farklı dosyalardır. Windows değildir.

---
*Hazırlayan: AÖF CS Topluluğu*
